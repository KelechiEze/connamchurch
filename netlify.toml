[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "18"

# This is critical - handles SPA routing without breaking assets
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200